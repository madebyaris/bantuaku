# ============================================
# Bantuaku - Environment Variables Example
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ============================================
# Backend Configuration
# ============================================

# Backend server port (default: 8080)
PORT=8080

# Database connection string
# Format: postgres://user:password@host:port/database?sslmode=disable
# For local development (without Docker):
DATABASE_URL=postgres://bantuaku:bantuaku_secret@localhost:5432/bantuaku_dev?sslmode=disable

# For Docker Compose (use service names):
# DATABASE_URL=postgres://bantuaku:bantuaku_secret@db:5432/bantuaku_dev?sslmode=disable

# Redis connection URL
# For local development (without Docker):
REDIS_URL=redis://localhost:6379

# For Docker Compose (use service name):
# REDIS_URL=redis://redis:6379

# JWT secret key for token signing (CHANGE THIS IN PRODUCTION!)
# Generate a secure random string: openssl rand -base64 32
JWT_SECRET=dev-jwt-secret-change-in-production

# Kolosal.ai API Key (for chat completions and OCR)
# Get your API key from: https://api.kolosal.ai
# Leave empty if you want to use mock responses
KOLOSAL_API_KEY=

# CORS allowed origin (frontend URL)
CORS_ORIGIN=http://localhost:3000

# Logging level: debug, info, warn, error (default: info)
LOG_LEVEL=info

# ============================================
# Frontend Configuration
# ============================================

# Backend API URL for frontend
# For local development (without Docker):
VITE_API_URL=http://localhost:8080

# For Docker Compose (use service name):
# VITE_API_URL=http://backend:8080

# Enable Docker mode (for hot reload in Docker)
# Set to "true" when running in Docker, leave empty otherwise
DOCKER=

# ============================================
# Database Configuration (for local PostgreSQL)
# ============================================
# These are only needed if running PostgreSQL locally (not in Docker)

# POSTGRES_DB=bantuaku_dev
# POSTGRES_USER=bantuaku
# POSTGRES_PASSWORD=bantuaku_secret
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432

# ============================================
# Development Notes
# ============================================
# 
# Quick Start:
# 1. Copy this file: cp .env.example .env
# 2. Add your KOLOSAL_API_KEY (optional, for AI features)
# 3. Update JWT_SECRET for production
# 4. Adjust DATABASE_URL and REDIS_URL if needed
#
# Docker Compose:
# - When using docker-compose, most variables are set automatically
# - You only need to set KOLOSAL_API_KEY in your .env file
# - docker-compose.yml will read from .env automatically
#
# Local Development:
# - Make sure PostgreSQL and Redis are running locally
# - Or use Docker Compose for database services only:
#   docker-compose up db redis
#
# Production:
# - Use strong, randomly generated JWT_SECRET
# - Set LOG_LEVEL=info or warn
# - Use secure database credentials
# - Enable SSL for database connections
